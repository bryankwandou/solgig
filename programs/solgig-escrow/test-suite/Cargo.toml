[package]
name = "solgig-escrow-tests"
version = "0.1.0"
edition = "2021"
publish = false

# Runs against ../target/deploy/solgig_escrow.so; build that first with
# `npm run program:build` (or cargo build-sbf in the parent directory).
[dev-dependencies]
mollusk-svm = "0.15.1"
solana-account = "4.3"
solana-pubkey = "4.1"
solana-instruction = "3.2"

[workspace]
